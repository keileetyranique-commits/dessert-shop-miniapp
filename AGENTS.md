# AGENTS.md — Codex 开发前置规则

本文件是仓库级开发指令。Codex、AI 编码代理和人工开发者在修改代码前必须先阅读并遵守。

## 每次开发开始前必须阅读

1. 本文件 `AGENTS.md`。
2. `docs/MERCHANT-UX-RULES.md`：商家易读化、老板模式、表单和图表规则。
3. `docs/OPEN-SOURCE-REUSE-PRIORITIES.md`：成熟开源项目优先级、可复用范围和许可证边界。
4. 当前 Phase 文档，以及与任务直接相关的 PRD/API/数据库/成本定价文档。
5. 先检查现有实现和测试，再决定是否新增代码；不得仅根据任务描述猜测仓库现状。

如果任务涉及商家后台、小程序、POS、经营分析、AI 研判、商品、价格、成本、库存、订单或营销，`docs/MERCHANT-UX-RULES.md` 是强制验收标准，不是建议。

## 产品总原则

### 1. 成熟能力优先复用，差异化能力重点自研

普通商家系统已有成熟做法的能力，例如后台布局、商品 CRUD、图片上传、表单、列表、搜索、筛选、订单状态展示、库存操作，应先寻找成熟开源实现和成熟餐饮/外卖商家系统的交互方式。

优先级：

1. 明确 MIT / Apache-2.0 / BSD 等可商用许可证，并且技术栈/交互适配的成熟开源项目。
2. 技术栈不同但业务流程成熟的项目：参考信息架构和交互，不硬搬不兼容后端。
3. 自研。

不得直接复制许可证不明确的代码；GPL/AGPL 等强 copyleft 项目未经明确决策不得直接引入。任何新增第三方代码或组件都要记录来源和许可证。

不要为了“自己写”而重复制造已有成熟轮子。若已有成熟可复用方案仍决定自研，开发报告中必须说明原因。

重点自研的差异化能力包括：

- 成本和利润保护；
- Pricing Engine；
- 商品判断和经营分析；
- 市场/竞品/热点分析；
- 原料和经营风险雷达；
- 超级经营研判和经营方案；
- 多 AI 智能调度、证据校验、方案复核；
- 商家确认后执行、暂停、重研判和复盘。

### 2. 小店优先、低成本优先

目标用户包含夫妻店、小餐饮店和非专业经营者。每增加一个持续花钱的功能，先问：

1. 商家真的需要吗？
2. 能否明显提高收入、降低成本或减少麻烦？
3. 有没有更便宜的方法做到约 80% 的效果？

服务器、AI、市场数据和第三方服务都按“够用、可扩展、按需消费”设计。不得为了架构漂亮提前引入昂贵基础设施。

### 3. AI 不是确定性计算器

金额、成本、利润、库存、价格边界、权限和订单安全规则必须由确定性代码/数据库/规则引擎计算。

AI 用于：

- 综合分析；
- 市场研判；
- 趋势解释；
- 风险发现；
- 新品和经营方案生成；
- 多方案比较和复核。

任何 AI 经营方案在进入“可执行”状态前，必须经过确定性规则、Pricing Engine/Cost Engine 和权限校验，并由商家确认。不得让 AI 自行修改关键经营配置。

AI 接入必须 Provider-agnostic，不绑死单一厂商。未来支持多个 API、BYOK、自定义 Base URL、主备/动态路由、预算上限和按需多模型复核。

### 4. 外部市场分析先本地筛选，再调用 AI

市场/竞品/网络热点/原料价格等数据不得整批直接发送给 AI。

默认流程：

原始/授权公开数据 → 标准化/去重 → 本地统计和异常筛选 → 生成小型证据包 → AI 研判 → 必要时第二模型复核 → 规则引擎验证 → 商家确认。

原始外部数据只按必要期限保留；长期保存轻量市场快照、趋势和证据摘要，避免无意义存储和重复 token 消耗。

### 5. 人能看懂优先于技术术语

商家界面默认使用中文、人话、图表、趋势和明确行动建议。

底层技术名词（Fen、bps、SKU 内部状态、Asia/Shanghai、计算版本、内部枚举等）除高级/诊断场景外，不直接暴露给普通商家。

所有 UI/UX 详细规则见 `docs/MERCHANT-UX-RULES.md`。

## 工程与安全规则

1. 每个 Phase/较大功能使用独立分支和 PR；文档级仓库规则可单独小改。
2. 先写/补测试，再完成功能；修 bug 必须补回归测试。
3. Secret 只能放服务端安全配置，绝不提交 GitHub，绝不传到小程序或浏览器。
4. 金额内部统一使用整数分（Fen）；用户界面使用人民币元并精确转换。
5. 百分比规则内部使用 bps/整数；用户界面显示正常百分比。
6. 支付、退款、配送回调等必须幂等。
7. Mock 能力必须由服务端总开关完全关闭，生产环境不得存在开发鉴权旁路。
8. 保持 Merchant → Brand → Store 多租户隔离和数据库约束。
9. 不修改已经落地的旧 migration；新增变更创建新 migration。
10. 每次 PR 完成后运行仓库规定的 lint / format / typecheck / test / build / migration / integration / compose 验证。
11. 未经人工审查和明确授权，不合并功能 PR。
12. 开发完成报告必须包含：复用了什么、为何自研、用户可见变化、测试、迁移、权限、安全、CI、未完成事项。

## 开发前快速检查清单

开始写代码前回答：

- 这个功能成熟项目是否已经有可复用实现？
- 普通商家是否真的需要看到这个字段？
- 这个字段能否由系统自动补全？
- 是必填还是选填？选填是否应该折叠？
- 是否出现专业术语？能否改成人话或图表？
- 金额是否以“元”展示、以“分”存储？
- 状态/单位/时间是否适合中国商家？
- 这项 AI/服务器/数据能力是否值得持续成本？
- 能否先用本地统计/规则完成，再调用 AI？
- 是否需要老板明确确认后才能改变经营策略？

只要以上任一问题没有处理清楚，就不要直接堆功能。


## 中文优先规则

1. 面向商家、用户、产品负责人和开发协作的方案、说明、计划、验收结论、PR 描述、Issue 描述和 Codex 最终报告，默认全部使用简体中文。
2. 用户可见界面、按钮、提示、错误信息、图表标题和帮助文案必须使用中文；除非行业中没有自然中文表达，否则不要直接显示英文术语。
3. 代码标识符、API 路径、数据库字段、第三方协议名、标准枚举和必须保持兼容的技术名称可以保留英文，但需要在文档或界面旁提供中文解释。
4. 不得为了显得专业而混用中英文。优先使用普通商家能理解的中文表达。
5. Codex 开发报告如果必须引用英文技术名，正文解释仍然使用中文。
